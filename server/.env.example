# Flask Secret Key (generate a random string for production)
SECRET_KEY=dev

# Firebase Admin SDK Credentials (JSON string)
# Copy the entire contents of firebase.json and paste as a single line here
# Or leave empty to use firebase.json file from project root
FIREBASE_CONFIG=
